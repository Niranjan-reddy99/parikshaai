{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin-allow-popups" }
      ]
    }
  ],
  "rewrites": [
    { "source": "/__/auth/:path*", "destination": "/api/auth/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
